{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "bash -lc \"python manage.py migrate && python manage.py collectstatic --noinput && gunicorn muviz.wsgi:application --bind 0.0.0.0:${PORT:-8080} --workers 2 --timeout 180\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
